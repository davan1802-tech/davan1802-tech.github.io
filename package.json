{
  "name": "danganhvan-resume-site",
  "private": true,
  "description": "Resume site; run npm run pdf to export vector PDF from HTML/CSS (Chromium print).",
  "scripts": {
    "pdf": "node scripts/generate-pdf.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
